靶子质量（用原始问句检索，警戒线 = rank 60）

  ✅ 接入层想限制单个 IP 每秒最多处理 2 个请求，同时允许短时间的突发，
      名次 [1, 24]　seq [75, 63]
  ✅ 我配了每秒 2 个请求，为什么桶里最多能攒下 3 个？这个数是怎么算出来
      名次 [2, 5]　seq [72, 69]
  ✅ 不想引第三方限流库，接口级别的并发控制用 Java 自己怎么实现？要按时
      名次 [5, 13]　seq [38, 39]
  ✅ 同一个限流需求，用 Java 代码、用 Redis 脚本、用 Nginx
      名次 [28, 9, 20]　seq [38, 49, 61]
  ✅ 我们的限流是按 IP 做的，如果接入层有好几台 Nginx，同一个 IP
      名次 [3, 6]　seq [81, 82]
  ✅ 漏桶和令牌桶，哪个允许突发？为什么两者实现一样效果却相反？
      名次 [4, 1]　seq [23, 36]
  ⚠ 用 RateLimiter 时，为什么有时候第一次不等、紧接着的请求却要
      名次 [6, 74]　seq [43, 46]
  ✅ Nginx 自带的两个限流模块各管什么？一个管连接数、一个管请求速率，关
      名次 [1, 15]　seq [56, 61]
  ✅ 被限流之后返回 503，怎么改成跳转到自己的错误页面？
      名次 [10]　seq [81]
  ✅ 最简单的那种计数器限流，在时间窗口边界上有什么漏洞？后来用什么办法补的？
      名次 [22, 26]　seq [27, 28]
  ✅ 单机限流、多机协同限流、入口层限流，这三层在文档里是分开讲的吗？各在哪？
      名次 [2, 9, 11]　seq [37, 48, 55]
  ✅ 令牌桶里令牌按什么节奏补？CIR 和 Tc 是什么意思？
      名次 [4, 1]　seq [8, 9]
  ✅ Redis 重启时如果 RDB 和 AOF 同时开着，它会用哪个文件恢复
      名次 [37, 19]　seq [8, 14]
  ⚠ bgsave 号称不阻塞，为什么数据集一大还是会卡顿一下？它靠什么保证快
      名次 [2, 103]　seq [6, 7]
  ✅ AOF 的刷盘策略有哪几种？最坏情况下会丢多少数据？
      名次 [29, 14]　seq [15, 18]
  ⚠ 生产环境里主库和从库分别该配哪种持久化？判断依据是什么？
      名次 [8, 157]　seq [21, 22]
  ✅ save 和 bgsave 有什么区别？阻塞具体发生在哪一步？
      名次 [1, 4]　seq [2, 3]
  ✅ appendfsync 三个取值分别意味着什么？最坏丢多少数据？哪个是默
      名次 [12, 2]　seq [8, 12]
  ✅ AOF 文件越来越大怎么办？什么条件会自动触发重写？
      名次 [3, 1]　seq [9, 10]
  ✅ RDB 和 AOF 的启动优先级谁高？这个对比表里还列了哪些维度？
      名次 [1]　seq [14]
  ✅ AOF 重写期间的新写命令会丢吗？重写是怎么切换文件的？
      名次 [5, 1]　seq [13, 11]
  ✅ 调度器把 Pod 放到节点上分哪两大步？每步各自的算法叫什么？
      名次 [2, 12]　seq [2, 3]
  ✅ 想让 Pod 尽量落在某个节点但不强制，YAML 用哪个字段？如果那个节
      名次 [9, 11]　seq [11, 14]
  ⚠ 节点亲和里的硬策略和软策略分别对应哪两个长字段名？行为差别是什么？
      名次 [11, 64]　seq [19, 16]
  ✅ 怎么表达「这个 Pod 要和某个 Pod 待在一起」？它和「落在某类节点
      名次 [6, 20]　seq [18, 24]

可疑靶子 4 个：
  rank=74　seq=46　「此处可以看到我们设置的桶容量为2（即允许」　用 RateLimiter 时，为什么有时候第一次不等、紧接
  rank=103　seq=7　「RDB操作借用 copy on writ」　bgsave 号称不阻塞，为什么数据集一大还是会卡顿一下？它
  rank=157　seq=22　「如果你的环境一切非常良好，且服务需要接收」　生产环境里主库和从库分别该配哪种持久化？判断依据是什么？
  rank=64　seq=16　「- weight: 1 \n prefe」　节点亲和里的硬策略和软策略分别对应哪两个长字段名？行为差别是
