{
 "doc": "JVM基础系列：G1垃圾回收器 - 梅晓煜 - 博客园.md",
 "prefix": "g1",
 "cases": [
  [1, [2, 25],
   "G1 把整个 Java 堆划分成多少个大小相同的 Region？划分的目标依据是什么？"],
  [2, [8, 9],
   "回收新生代时为了不全堆扫描，G1 用了哪个数据结构？它又靠什么机制知道某个引用跨了 Region？"],
  [3, [11, 10],
   "年轻时回收时 G1 会对 dirty card queue 做什么？文档说这个结果最终会被加到什么地方、从而避免全局扫描？"],
  [4, [14, 15],
   "并发标记结束后，「部分为垃圾」的老年代分段默认分几次被回收、由哪个参数控制？而混合回收并不一定要做满这个次数，提前停下来的那个阈值参数叫什么、默认多少？"],
  [5, [16, 20],
   "文档说触发 Full GC 的原因有哪两个？另外那种「巨型对象找不到合适空间」的情形叫什么、会引发什么？"],
  [6, [22, 26],
   "触发并发标记周期的堆占用比率由哪个参数设定、默认值多少？而决定一个 Region 是否值得纳入回收集的「活跃度阈值」是哪个参数、文档说它的默认值有哪两个版本？"],
  [7, [3, 5],
   "文档怎么区分 G1 的「并行性」与「并发性」？而在内存占用与额外执行负载上，它跟 CMS 比是高还是低？"],
  [8, [24, 27],
   "文档说的 Cset（回收集）是什么？每轮 Mixed GC 附加进 Cset 的老年代 Region 最多能占全部 Region 的多少？"],
  [9, [12, 13],
   "并发标记周期里，第一阶段和第二阶段各是不是 STW？G1 用什么算法解决并发标记中的「漏标」问题？"],
  [10, [6, 1],
   "G1 面向的是什么类型的应用（对内存和处理器的要求）？文档说它在普通大小的堆里表现如何？"]
 ]
}
