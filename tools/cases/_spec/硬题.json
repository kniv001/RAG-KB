{
 "doc": "",
 "prefix": "hd",
 "multi": true,
 "note": "按诊断写的硬题：**问句不用靶子里的词**，改从场景/症状切入。每条自带 doc。",
 "cases": [
  ["JVM基础系列：G1垃圾回收器 - 梅晓煜 - 博客园.md", 1, [8, 9],
   "线上服务的 Young GC 一次比一次慢，日志显示每次都要扫很大的范围。文档说 G1 靠哪个机制来避免这种全堆扫描？它又是怎么知道某个引用跨了 Region？"],
  ["JVM基础系列：G1垃圾回收器 - 梅晓煜 - 博客园.md", 2, [19, 18],
   "把堆调大之后 Full GC 反而更频繁，日志里出现「to-space exhausted」。文档列的那几种会引发 Full GC 的情形里，这属于哪一类？它给的两条缓解办法是什么？"],
  ["JVM基础系列：G1垃圾回收器 - 梅晓煜 - 博客园.md", 3, [21, 22],
   "把最大停顿目标调得很小之后吞吐反而掉了。文档说 G1 为了满足这个停顿目标会尝试哪些弥补手段？它又是在堆占用达到哪个比率时启动后台处理的？"],
  ["Rust——所有权【四】 - 。思索 - 博客园.md", 4, [15, 12],
   "同事说 Rust 里 `let s2 = s1` 之后 s1 就不能用了，他觉得这跟别的语言的浅拷贝差不多。文档为什么说这个类比不准确？如果两个指针都去释放同一块内存会出什么错？"],
  ["Rust——所有权【四】 - 。思索 - 博客园.md", 5, [10, 9],
   "Rust 没有 GC，可也不会忘记释放内存。它靠什么时机触发释放？文档说在没有 GC 的语言里，这件事原本要由谁负责、难在哪？"],
  ["深入解析：人工智能基础架构与算力之5 向量数据库技术原理：支撑大模型语义检索的关键架构 - clnchanpin - 博.md", 6, [6, 2],
   "同事推荐用哈希类索引，说吞吐能到 3000+ QPS。按文档给的基准，这个选择在哪个指标上会吃亏？而这几类索引共同的取舍是什么？"],
  ["深入解析：人工智能基础架构与算力之5 向量数据库技术原理：支撑大模型语义检索的关键架构 - clnchanpin - 博.md", 7, [17, 15],
   "商品描述向量化之后，用户按「适合两岁宝宝的礼物」这类说法还是搜不到想要的东西。文档说带过滤条件的检索，过滤器是在流程的哪一步应用的？检索出候选之后还能做什么来提升准确性？"],
  ["TCP拥塞窗口详解：慢启动、避免、恢复策略-CSDN博客.md", 8, [9, 10],
   "抓包看到发送方收到一批重复 ACK 之后把窗口砍了一半。这是哪个动作？文档说 Reno 与它的改进版在这个阶段的做法差在哪、改进版要等到什么条件才退出？"],
  ["mysql事务隔离级别及MVCC 原理 - 一只阿木木 - 博客园.md", 9, [10, 11],
   "同一个事务里两次查同一行结果不一样；换成另一个隔离级别就一致了。文档说这两个级别在「什么时候生成 ReadView」上的差别是什么？"],
  ["raft一致性算法详解：CAP理论、选举机制与日志复制-CSDN博客.md", 10, [7, 6],
   "集群里所有节点同时超时，结果谁也没选上 leader，反复重来。文档把这个僵局叫什么？RAFT 用什么办法规避它？"]
 ]
}
