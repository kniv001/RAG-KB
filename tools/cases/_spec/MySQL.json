{
 "doc": "mysql事务隔离级别及MVCC 原理 - 一只阿木木 - 博客园.md",
 "prefix": "mv",
 "cases": [
  [1, [2, 3],
   "文档给「脏读」和「不可重复读」各下了什么定义？而幻读与不可重复读，文档说快照读是用什么机制避免的？"],
  [2, [5, 6],
   "MVCC 设计出来是为了解决哪两个问题、换来了什么好处？在读未提交这个级别下，事务 B 直接读的是版本链上的哪一部分？"],
  [3, [7, 8],
   "ReadView 里的 min_trx_id 和 max_trx_id 分别是什么？文档给的可见性判断里，第一步比的是当前版本的 trx_id 与哪个字段？"],
  [4, [9, 12],
   "某个版本对当前事务不可见时，下一步怎么走？如果命中的是二级索引页，文档说可以拿哪个字段做一次快速判断、判断出什么？"],
  [5, [10, 11],
   "读已提交与可重复读在「什么时候生成 ReadView」上有什么差别？文档用事务 300 举的例子说明了这一点吗？"],
  [6, [13, 14],
   "update 语句改的是主键或索引字段时，文档说会先做什么动作？而 undo 日志会不会永远不释放、释放的时机由什么决定？"],
  [7, [15, 16],
   "在读已提交级别下，事务 1 的 ReadView1 什么时候不再是活跃的？ReadView 上的「事务 no」属性和活跃 ReadView 链是用来做什么的？"]
 ]
}
