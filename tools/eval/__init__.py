# -*- coding: utf-8 -*-
"""**模型测评模块**（小型测评平台）—— 与管线尺子分开。

为什么要分开：
  · `tools/ruler/` 量的是**语料与管线**（检索排第几、分段切得准不准、召不召得回）——
    它**与模型无关**，报告带的是**语料戳**
  · 这里量的是**模型本身**（分类判断、引用纪律、诚实性、速度）——
    报告带的是**模型名**，换模型就该重跑

分工按"什么变了要重跑"划：语料变了重跑 ruler；换模型了重跑 eval。

平台的最小形状就是三件事：**声明式基准**（`benches/*.json`）+
**一等公民 `--model`** + **可比的报告**（带模型名、可复算）。
"""
from . import judges, runner   # noqa: F401
