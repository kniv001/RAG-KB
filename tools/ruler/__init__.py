# -*- coding: utf-8 -*-
"""尺子框架：语料层 / 题目层 / 判据层 / 检索层 / 报告层。

设计原则（都是从 2026-09-20 那次事故里长出来的）：
  · 靶子**锚内容不锚位置** —— 语料重建不用改题目
  · 缓存在**语料戳**上对齐 —— 错位不会静默
  · 每个数字都带**尺子名 + 语料戳** —— 判死不能脱离尺子引用
  · 跑之前先**审计**，不过就拒跑 —— 错数比没数坏
"""
from . import cases, corpus, judges, report, retr   # noqa: F401
